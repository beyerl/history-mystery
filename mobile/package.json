{
  "name": "history-mystery-mobile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Capacitor wrappers that package the quiz web frontends as Android apps.",
  "scripts": {
    "assemble": "node assemble.mjs",
    "add:android": "cap add android",
    "sync": "cap sync android",
    "icons": "capacitor-assets generate --android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2"
  }
}
