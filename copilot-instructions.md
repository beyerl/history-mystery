# Javascript
- Use Javascript Web Components whenever possible
- Every Webcomponent needs to have a render function
- The Render function should return the component
- Do not use typescript or a js framework
- javascript filenames are in kebap case
- Use HTML templating syntax instead of javascript syntax to define dom element whenever possible