// Deployment config (kept separate so it can be rewritten per environment).
// Techno Mystery reuses the same GameState API for multiplayer.
export const API_BASE_ADDRESS = 'https://localhost:7227/';
